<%= render "shared/header" %>
<h1 class="subject"><%= current_student.first_name+"様 検索結果一覧ページ" %></h1>

<% if @student_search_businesses.present? %>
  <%= render "shared/businesslist"%>
<% else %>
  <h3>該当する業務はありません。</h3>
<%end%>

<%= render "shared/footer" %>