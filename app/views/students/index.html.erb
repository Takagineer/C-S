<%= render "shared/header" %>
<h1 class="subject" ><%= current_student.first_name+"様 マイページ" %></h1>
<h2>業務検索機能</h2>
  <%= form_with url: search_student_path(current_student.id), method: :get, local: true do |f| %>
    <%= f.text_field :keyword, placeholder: "投稿を検索する" %>
    <%= f.submit "検索" %>
  <% end %>

<h1 class="subject" >応募した業務一覧</h1>

<table>
  <tr>
    <th>契約中業務</th>
  </tr>

  <tr> 
    <td></td>
  </tr>

  <tr>
    <th>やりとり中業務</th>
  </tr>

  <tr>
    <th>契約終了した業務</th>
  </tr>

  <tr>
    <th>お見送りになった業務</th>
  </tr>

</table> 

<%= render "shared/footer" %>